<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      <br>
      URL: <%= link.url %>
      <br>
      Tags: 
      <% unless link.tags.empty? %>
        <% link.tags.each_with_index do |tag, i| %>
          <%= tag.name %>
        <% end %>
      <% end %>
    </li><br>
  <% end %>
</ul>

<a href='/links/new'>Add Bookmark</a>


