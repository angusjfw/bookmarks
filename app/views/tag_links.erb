<h1><%= @tag.name.capitalize %></h1>

<ul id='links'>
  <% @links.each do |link| %>
  <li>
    <%= "<a href='https://#{link.url}'>" + link.title + "</a>" %>
  </li>
  <% end %>
</ul>

<a href='/links'>All Bookmarks</a>
